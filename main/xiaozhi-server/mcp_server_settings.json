{
  "description": [
    "Copy this file to data/.mcp_server_settings.json only when external MCP servers are needed.",
    "The server accepts stdio entries with command/args and remote entries with url/transport/headers.",
    "Keep mcpServers empty to leave external MCP disabled."
  ],
  "mcpServers": {}
}
